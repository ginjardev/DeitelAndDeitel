package testDrivenDevelopment;

public class ArrayMain {
    public static void main(String[] args) {
        /*int[] arrays = {1, 2, 3, 4, 5};

        for(int counter = 0; counter < arrays.length; counter++){
            //arrays[counter] = arrays[counter] * 3;

            System.out.println(arrays[counter]);
        }*/

        /*System.out.println("sum of array elements = " + Array.getSumOfArrayElements(arrays));

        System.out.println("Average of array elements = " + Array.getAverageOfArrayElements(arrays));

        System.out.println("Maximum number in array of elements = " + Array.getMaximumNumberInArray(arrays));

        System.out.println("Minimum number in array of elements = " + Array.getMinimumNumberInArray(arrays));

        System.out.println("this maximum = " + (Array.getSumOfArrayElements(arrays) - Array.getMinimumNumberInArray(arrays)));

        System.out.println("this  minimum = " + (Array.getSumOfArrayElements(arrays) - Array.getMaximumNumberInArray(arrays)));*/

         //Array.diceRollCount();
         //Array.multiArray();
         Array.diceRollCounter();
    }
}
